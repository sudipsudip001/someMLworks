## Contract/Policy Inconsistency Detector

This is an LLM agent made using Langchain that checks if the document has inconsistencies in its making or not.

The agent must transform unstructured text into structured rule system and detect contradictions across dimensions.